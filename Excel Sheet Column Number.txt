class Solution {
    public int titleToNumber(String a) {
        int sol = 0;
        for (int i=0; i<a.length(); i++){
                sol = sol*26 + (a.charAt(i)-64);
        }
        return sol;
    }
}